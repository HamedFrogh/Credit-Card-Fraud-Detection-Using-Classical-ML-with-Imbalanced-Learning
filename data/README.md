# Data Folder Instructions

Place the Kaggle credit card fraud dataset CSV in this folder with the exact name:

- `creditcard.csv`

Expected final path:
- `data/creditcard.csv`

The training command used by this project:

```bash
python -m src.train --data-path data/creditcard.csv --output-dir outputs
```
